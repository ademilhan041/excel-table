package com.adem.tool.export.xls

enum class ElementAlignment { LEFT, CENTER, RIGHT }
enum class ElementWeight { NORMAL, BOLD }
enum class ElementSize { H1, H2, H3, H4, H5, H6 }
enum class Color { BLACK, WHITE, TAN }
enum class Type { NUMBER, DOUBLE, DECIMAL, DATE, DATETIME, IMAGE }
enum class ImageExt { JPEG, PNG }
